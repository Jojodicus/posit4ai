[project]
name = "fpga-inference-client"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "opencv-python",
    "numpy",
    "requests",
    "pygobject",
    "torch",          # only needed for .pt weight loading
]

[dependency-groups]
dev = []
